name: Build & Test

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: Install Dependencies
        run: npm install
        
  test:
    runs-on: ubuntu-latest
    steps:
      - name: hello
        run: echo "${env}"
